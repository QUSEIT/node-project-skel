// a week
const ACCESS_TOKEN_TIMEOUT = 7 * 24 * 60 * 60;
// a month
const REFRESH_TOKEN_TIMEOUT = 30 * 24 * 60 * 60;
const ACCESS_TYPE = "access";
const REFRESH_TYPE = "refresh";

module.exports = {
  ACCESS_TOKEN_TIMEOUT,
  REFRESH_TOKEN_TIMEOUT,
  ACCESS_TYPE,
  REFRESH_TYPE
}